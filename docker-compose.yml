services:
  ui:
    build:
      context: "./ui"
      dockerfile: "Dockerfile"
    ports:
      - 8080:80
    environment:
      - NGINX_HOST=cbarchvm01
      - NGINX_PORT=8080
  api:
    build:
      context: "./api"
      dockerfile: "Dockerfile"
    ports:
      - 443:443
      - 80:80
